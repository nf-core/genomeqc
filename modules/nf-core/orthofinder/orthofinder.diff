Changes in module 'nf-core/orthofinder'
--- modules/nf-core/orthofinder/main.nf
+++ modules/nf-core/orthofinder/main.nf
@@ -42,6 +42,11 @@
     if [ -e ${prior_run}/OrthoFinder/Results_$prefix ]; then
         mv ${prior_run}/OrthoFinder/Results_$prefix $prefix
     fi
+
+    if [ ! -s "$prefix/Orthogroups/Orthogroups.tsv" ]; then
+        echo "ERROR: orthofinder did not produce $prefix/Orthogroups/Orthogroups.tsv - see the log above for the underlying failure." >&2
+        exit 1
+    fi
     """
 
     stub:

************************************************************
