/*
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    nf-core/genomeqc Nextflow config file
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Default config options for all compute environments
----------------------------------------------------------------------------------------
*/

// Global default params, used in configs
params {

    // Input options
    input                      = null

    // tidk options
    skip_tidk                  = false
    repeat                     = null // Repeat string

    // seqkit options
    save_sorted_seqs           = false
    save_filtered_seqs         = false

    // Genome only option
    genome_only                = null

    // AGAT options
    save_validated_annotation  = false

    // ncbigenomedownload options
    groups                     = 'all'
    save_assembly              = false

    // GFFREAD options
    save_extracted_seqs        = false

    // orthofinder options
    save_orthofinder_results   = true
    ortho_version              = 'v3'

    // Longest options
    save_longest_isoform       = false

    // merqury/meryl options
    kvalue                     = 21

    // Annotation options
    val_tool                  =  'agat'

    // BUSCO options
    busco_lineage              = 'hymenoptera_odb10'
    busco_lineages_path        = null
    busco_config               = null
    busco_clean                = null
    min_buscos                  = null // // Minimum number of Complete BUSCOs a sequence must have to be counted
    skip_busco                 = false

    // Tree summary options
    tree_scale                 = null
    tree_margin                = null
    tree_style                 = "roundrect" // Tree layout style: 'roundrect' (rounded branches, default), 'ellipse' (curved branches with node points), 'rectangular' (legacy dotted leader lines), or 'circular' (fan tree with stats as concentric rings)
    circular_rings             = null // Circular layout only: comma-separated ordered stats to draw as rings (inner->outer), or 'all'. Null uses a curated default. Keys: ch_plot,len_plot,n50_plot,gene_plot,busco_gen_plot,busco_prot_plot,busco_dup_plot,nseqs_plot,ortho_plot
    quality_preset             = null // Circular layout only: phylo-group thresholds for the quality (traffic-light) rings: generic (default, lenient), vertebrate, insect, plant, fungi, bacteria
    quality_thresholds         = null // Circular layout only: override individual --quality_preset cut-offs, e.g. 'n50=2e6:5e5,seq_number=500:5000'. Metrics: busco_complete, busco_duplicated, n50, seq_number
    show_ring_values           = false // Circular layout only: print each value on its ring (redundant, non-colour-only encoding)
    skip_plots_genome_anno     = "nseqs_plot" //'nseqs_plot' // Comma separated list (e.g. 'ch_plot,gene_plot'). Possible values: 'ch_plot', 'nseqs_plot', 'ortho_plot', 'len_plot', 'gene_plot', 'n50_plot', 'pies_plot'
    skip_plots_genome_only     = "nseqs_plot" //'nseqs_plot' // Comma separated list (e.g. 'ch_plot,len_plot'). Possible values: 'ch_plot', 'nseqs_plot', 'ortho_plot', 'len_plot', 'n50_plot', 'pies_plot'

    // Decontamination options
    gxdb_manifest              = null // http to fetch gxdb databse
    gxdb                       = null // path to gxdb databse
    ramdisk                    = null // path to tmpfs or ramfs if available
    save_cleaned_genome        = false // save cleaned genome after decontamination
    save_cleaned_adaptor       = false // save cleaned genome after adaptor removal

    // Shiny app options
    container_engine           = "docker"

    // TE annotation options
    te                         = null  // TE annotation method: 'hite' or 'repeatmasker' (null = skip)

    // HiTE options (used when te = 'hite')
    is_plant                   = false // is it a plant genome
    curated_lib                = null

    // RepeatMasker options (used when te = 'repeatmasker')
    // -- DFAM curated library (provide one of the two options below) --
    famdb_library              = null  // path to pre-staged directory of DFAM h5 partitions; skips download when set
    famdb_lineage              = null  // lineage to extract from famdb (e.g. 'hymenoptera', 'viridiplantae')
    RM_download_db             = false  // download DFAM h5 partitions automatically (ignored when famdb_library is set)
    RM_db                      = ["https://www.dfam.org/releases/Dfam_3.9/families/FamDB/dfam39_full.0.h5.gz"] // DFAM partition URLs (used when RM_download_db = true and famdb_library is null)
    // -- de novo repeat discovery (optional, very slow) --
    run_repeatmodeler          = false // run RepeatModeler to build a de novo library (adds 24-48 h per genome)
    repeatmasker_speed         = 'qq'  // RepeatMasker sensitivity: 'default' (most sensitive), 'q' (quick), 'qq' (rush, fastest)
    // -- repeat library clustering --
    te_clusterer               = 'linclust' // 'linclust' (default), 'mmseqs', or 'cdhit'
    te_cluster_identity        = 0.8
    te_cluster_coverage        = 0.8

    // MultiQC options
    multiqc_config             = null
    multiqc_title              = null
    multiqc_logo               = null
    max_multiqc_email_size     = '25.MB'
    multiqc_methods_description = null

    // Boilerplate options
    outdir                       = null
    publish_dir_mode             = 'copy'
    email                        = null
    email_on_fail                = null
    plaintext_email              = false
    monochrome_logs              = false
    help                         = false
    help_full                    = false
    show_hidden                  = false
    version                      = false
    pipelines_testdata_base_path = 'https://raw.githubusercontent.com/nf-core/test-datasets/'
    trace_report_suffix          = new java.util.Date().format( 'yyyy-MM-dd_HH-mm-ss')

    // Config options
    config_profile_name        = null
    config_profile_description = null

    custom_config_version      = 'master'
    custom_config_base         = "https://raw.githubusercontent.com/nf-core/configs/${params.custom_config_version}"
    config_profile_contact     = null
    config_profile_url         = null

    // Schema validation default options
    validate_params            = true
}

// Backwards compatibility for publishDir syntax
outputDir = params.outdir
workflow.output.mode = params.publish_dir_mode

// Load base.config by default for all pipelines
includeConfig 'conf/base.config'

profiles {
    debug {
        dumpHashes              = true
        process.beforeScript    = 'echo $HOSTNAME'
        cleanup                 = false
        nextflow.enable.configProcessNamesValidation = true
    }
    conda {
        conda.enabled           = true
        docker.enabled          = false
        singularity.enabled     = false
        podman.enabled          = false
        shifter.enabled         = false
        charliecloud.enabled    = false
        conda.channels          = ['conda-forge', 'bioconda']
        apptainer.enabled       = false
    }
    mamba {
        conda.enabled           = true
        conda.useMamba          = true
        docker.enabled          = false
        singularity.enabled     = false
        podman.enabled          = false
        shifter.enabled         = false
        charliecloud.enabled    = false
        apptainer.enabled       = false
    }
    docker {
        docker.enabled          = true
        conda.enabled           = false
        singularity.enabled     = false
        podman.enabled          = false
        shifter.enabled         = false
        charliecloud.enabled    = false
        apptainer.enabled       = false
        docker.runOptions       = '-u $(id -u):$(id -g)'
    }
    arm64 {
        process.arch            = 'arm64'
        // TODO https://github.com/nf-core/modules/issues/6694
        // For now if you're using arm64 you have to use wave for the sake of the maintainers
        // wave profile
        apptainer.ociAutoPull   = true
        singularity.ociAutoPull = true
        wave.enabled            = true
        wave.freeze             = true
        wave.strategy           = 'conda,container'
    }
    emulate_amd64 {
        docker.runOptions       = '-u $(id -u):$(id -g) --platform=linux/amd64'
    }
    singularity {
        singularity.enabled     = true
        singularity.autoMounts  = true
        conda.enabled           = false
        docker.enabled          = false
        podman.enabled          = false
        shifter.enabled         = false
        charliecloud.enabled    = false
        apptainer.enabled       = false
    }
    podman {
        podman.enabled          = true
        conda.enabled           = false
        docker.enabled          = false
        singularity.enabled     = false
        shifter.enabled         = false
        charliecloud.enabled    = false
        apptainer.enabled       = false
    }
    shifter {
        shifter.enabled         = true
        conda.enabled           = false
        docker.enabled          = false
        singularity.enabled     = false
        podman.enabled          = false
        charliecloud.enabled    = false
        apptainer.enabled       = false
    }
    charliecloud {
        charliecloud.enabled    = true
        conda.enabled           = false
        docker.enabled          = false
        singularity.enabled     = false
        podman.enabled          = false
        shifter.enabled         = false
        apptainer.enabled       = false
    }
    apptainer {
        apptainer.enabled       = true
        apptainer.autoMounts    = true
        conda.enabled           = false
        docker.enabled          = false
        singularity.enabled     = false
        podman.enabled          = false
        shifter.enabled         = false
        charliecloud.enabled    = false
    }
    wave {
        apptainer.ociAutoPull   = true
        singularity.ociAutoPull = true
        wave.enabled            = true
        wave.freeze             = true
        wave.strategy           = 'conda,container'
    }
    gpu {
        docker.runOptions       = '-u $(id -u):$(id -g) --gpus all'
        apptainer.runOptions    = '--nv'
        singularity.runOptions  = '--nv'
    }
    test         { includeConfig 'conf/test.config'         }
    test_gca     { includeConfig 'conf/test_gca.config'     }
    test_nofastq { includeConfig 'conf/test_nofastq.config' }
    test_full    { includeConfig 'conf/test_full.config'    }
    test_genomeonly { includeConfig 'conf/test_genomeonly.config' }
    test_decon   { includeConfig 'conf/test_decon.config' }
    test_hymen   { includeConfig 'conf/test_hymen.config' }
    test_te      { includeConfig 'conf/test_te.config'      }
}

// Load nf-core custom profiles from different institutions

// If params.custom_config_base is set AND either the NXF_OFFLINE environment variable is not set or params.custom_config_base is a local path, the nfcore_custom.config file from the specified base path is included.
// Load nf-core/genomeqc custom profiles from different institutions.
includeConfig params.custom_config_base && (!System.getenv('NXF_OFFLINE') || !params.custom_config_base.startsWith('http')) ? "${params.custom_config_base}/nfcore_custom.config" : "/dev/null"

// Load nf-core/genomeqc custom profiles from different institutions.
includeConfig params.custom_config_base && (!System.getenv('NXF_OFFLINE') || !params.custom_config_base.startsWith('http')) ? "${params.custom_config_base}/pipeline/rnaseq.config" : "/dev/null"

// Set default registry for Apptainer, Docker, Podman, Charliecloud and Singularity independent of -profile
// Will not be used unless Apptainer / Docker / Podman / Charliecloud / Singularity are enabled
// Set to your registry if you have a mirror of containers
apptainer.registry    = 'quay.io'
docker.registry       = 'quay.io'
podman.registry       = 'quay.io'
singularity.registry  = 'quay.io'
charliecloud.registry = 'quay.io'

// Load igenomes.config if required
//includeConfig !params.igenomes_ignore ? 'conf/igenomes.config' : 'conf/igenomes_ignored.config'

// Export these variables to prevent local Python/R libraries from conflicting with those in the container
// The JULIA depot path has been adjusted to a fixed path `/usr/local/share/julia` that needs to be used for packages in the container.
// See https://apeltzer.github.io/post/03-julia-lang-nextflow/ for details on that. Once we have a common agreement on where to keep Julia packages, this is adjustable.

env {
    PYTHONNOUSERSITE = 1
    R_PROFILE_USER   = "/.Rprofile"
    R_ENVIRON_USER   = "/.Renviron"
    JULIA_DEPOT_PATH = "/usr/local/share/julia"
}

// Set bash options
process.shell = [
    "bash",
    "-C",         // No clobber - prevent output redirection from overwriting files.
    "-e",         // Exit if a tool returns a non-zero status/exit code
    "-u",         // Treat unset variables and parameters as an error
    "-o",         // Returns the status of the last command to exit..
    "pipefail"    //   ..with a non-zero status or zero if all successfully execute
]

// Disable process selector warnings by default. Use debug profile to enable warnings.
nextflow.enable.configProcessNamesValidation = false

timeline {
    enabled = true
    file    = "${params.outdir}/pipeline_info/execution_timeline_${params.trace_report_suffix}.html"
}
report {
    enabled = true
    file    = "${params.outdir}/pipeline_info/execution_report_${params.trace_report_suffix}.html"
}
trace {
    enabled = true
    file    = "${params.outdir}/pipeline_info/execution_trace_${params.trace_report_suffix}.txt"
}
dag {
    enabled = true
    file    = "${params.outdir}/pipeline_info/pipeline_dag_${params.trace_report_suffix}.html"
}

manifest {
    name            = 'nf-core/genomeqc'
    contributors    = [
        [
            name: 'Chris Wyatt',
            affiliation: 'University College London',
            github: 'chriswyatt1',
            contribution: ['author', 'maintainer'],
            orcid: '0000-0001-8033-2213'
        ],
        [
            name: 'Fernando Duarte',
            affiliation: 'University College London',
            github: 'FernandoDuarteF',
            contribution: ['author', 'maintainer'],
            orcid: '0009-0003-2093-1734'
        ],
        [
            name: 'Stephen Turner',
            affiliation: 'Colossal Biosciences',
            github: 'stephenturner',
            contribution: ['author', 'contributor'],
            orcid: ''
        ],
        [
            name: 'Felipe Perez Cobos',
            affiliation: 'Institute of Agrifood Research and Technology (IRTA)',
            github: 'fperezcobos',
            contribution: ['author', 'contributor'],
            orcid: '0000-0001-5932-880X'
        ],
        [
            name: 'Lauren Huet',
            affiliation: 'Schmidt Ocean Institute',
            github: 'LaurenHuet',
            contribution: ['author', 'contributor'],
            orcid: '0009-0000-9575-9818'
        ],
        [
            name: 'Simon Murray',
            affiliation: 'Nextflow Ambassador',
            github: 'SimonDMurray',
            contribution: ['author', 'contributor'],
            orcid: ''
        ],
        [
            name: 'Jacques Dainat',
            affiliation: 'IRD',
            github: 'Juke34',
            contribution: ['author', 'contributor'],
            orcid: '0000-0002-6629-0173'
        ],
        [
            name: 'Mahesh Binzer-Panchal',
            affiliation: 'National Bioinformatics Infrastructure Sweden (NBIS)',
            github: 'mahesh-panchal',
            contribution: ['contributor'],
            orcid: '0000-0003-1675-0677'
        ],
        [
            name: 'Usman Rashid',
            affiliation: 'The New Zealand Institute for Plant and Food Research',
            github: 'GallVp',
            contribution: ['contributor'],
            orcid: '0000-0002-1109-5493'
        ],
    ]
    homePage        = 'https://github.com/nf-core/genomeqc'
    description     = """A pipeline to compare multiple genomes and annotations"""
    mainScript      = 'main.nf'
    defaultBranch   = 'master'
    nextflowVersion = '!>=25.10.4'
    version         = '1.0.0'
    doi             = ''
}

// Nextflow plugins
plugins {
    id 'nf-schema@2.5.1' // Validation of pipeline parameters and creation of an input channel from a sample sheet
}

validation {
    defaultIgnoreParams = ["genomes"]
    monochromeLogs = params.monochrome_logs
}
// Load modules.config for DSL2 module specific options
includeConfig 'conf/modules.config'
